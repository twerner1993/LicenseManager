@*@model IEnumerable<LicenseManagerMvc.Models.Lizenz>*@
@model LicenseManagerMvc.Models.ProgrammViewModel

<p>
    @Html.ActionLink("Create New", "Create", "Lizenz")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lizenzs)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lizenzs)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.Lizenzs) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Programm.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Edition)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Schluessel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LizenzId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LizenzId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LizenzId })
        </td>
    </tr>
}

</table>
